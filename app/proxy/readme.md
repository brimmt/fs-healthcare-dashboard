This folder will be focused on creating a reverse proxy in order to keep the legacy SOAP backend concelead. 

mappings.py (normalizing SOAP field -> REST fields) = Because SOAP uses uppercase keys, weird structures or legacy naming